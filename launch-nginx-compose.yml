version: '2'  
services:  
  nginx:
    image: nginx:latest
    container_name: nginx01
    network_mode: host
    restart: always
    volumes:
      - ./src:/src
      - ./config/nginx:/etc/nginx/conf.d
    extra_hosts:
      - "slmweb: 192.168.203.121"
    expose:
      - "8888"
